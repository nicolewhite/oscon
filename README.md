# OSCON
Files needed for the OSCON workshop.

```
$ pip install flask
$ pip install py2neo
$ pip install passlib
$ pip install bcrypt
```

```
$ git clone https://github.com/nicolewhite/oscon.git
$ cd oscon
$ python run.py
```
